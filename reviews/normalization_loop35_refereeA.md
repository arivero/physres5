# Normalization Loop 35 Referee A

## central claim

O17 is acceptable as a conditional theorem target.  Once O1, O3, O8,
and O10 supply one source route with common normalization, scalar vacuum
map, \(U(1)_{\rm em}\) embedding, and pole/running matching, the statement
\[
\alpha_{\mathcal S}(Q_\alpha)
=
\frac{g_{\rm sec}^2\sin^2\theta_{dV}}{4\pi}
+\Delta_{\alpha,{\rm match}}(Q_\alpha)
\]
becomes a testable endpoint relation.

## strongest source-supported result

The current text has good scheme discipline.  Target VII and Appendix E state
that Witten supports a geometric \(SU(3)\times U(1)\) endpoint, while Standard
Model electromagnetic embedding, charge lattice, generator normalization, and
four-dimensional kinetic normalization remain open.  Logan and Dawson support
\(M_W^2=g^2v^2/4\) and \(e=g\sin\theta_W\).  Martin--Robertson and
Jegerlehner support the running-scheme, threshold, and hadronic-vacuum-
polarization audit.

## weakest inference

The fragile step is
\[
v^2=C_v\mu^2|x_-(J_v)|,\qquad J_v=2,\qquad C_v=2 .
\]
It imports the negative branch into the Higgs vacuum normalization and then
uses that datum to define \(g_{\rm sec}\).  O3 still has to derive this scalar
functional, and O1 still has to derive the common W/Z/source normalization.

## hidden assumptions

- The same \(\mu\), inner product, and projection normalize
  \(x_+(3/4)\), \(x_+(2)\), and \(x_-(2)\).
- The spectral \(\sin^2\theta_{dV}\) enters the same scheme relation as
  \(e=g\sin\theta_W\), with a controlled matching correction.
- The \(D=9\) \(U(1)_{\rm geom}\) is Standard Model \(U(1)_{\rm em}\), with
  the observed charge lattice.
- The matching scale \(Q_\alpha\) is fixed by source data, including
  thresholds.
- The compact gauge kinetic integral fixes the normalized four-dimensional
  coupling after moduli stabilization.

## exact revisions needed

- Keep O17 prose in theorem-target language.
- Split the matching remainder into visible pieces:
  \[
  \sin^2\theta_{\mathcal S}(Q)
  =
  \sin^2\theta_{dV}+\Delta_\theta(Q),
  \qquad
  \alpha_{\mathcal S}(Q)
  =
  \frac{g_{\rm sec}^2\sin^2\theta_{\mathcal S}(Q)}{4\pi}
  +\Delta_{\rm th}(Q).
  \]
- State explicitly that the Witten five-sphere \(U(1)\) is geometric pending
  derivation of the Standard Model charge lattice and generator normalization.
- Add a source-audited table requirement for \(\alpha(0)\),
  \(\hat\alpha(\mu)\), effective \(\alpha(q^2)\), Euclidean Adler-function
  convention, thresholds, and \(Q_\alpha\).

# Loop 42 Random Recall

## Random Notes Reviewed

- `notes/lean/O1OrderedSampling.lean` records the same-source package
  \(u_{\rm EW}\mapsto(\mathcal H_J,\langle\cdot,\cdot\rangle,\Lambda_J,
  P_W,P_Z,P_\gamma,\mathcal R_{\rm pole},\mathcal F_{\rm sc})\).
- `notes/lean/DimensionalInterpolation.lean` records the shared source
  variable for \(t_{\rm dim}\), \(t_{\rm EW}\), \(\Lambda_J\), \(\widehat K_J\),
  \(\mathcal F_{\rm sc}\), and \(\mathcal Y_{\rm top}\).
- `notes/lean/ParentDirectoryLoop33.lean` and
  `notes/lean/ParentDirectoryLoop34.lean` preserve the parent-read protocol,
  CHM ledger priority, KK \(7/6/5\) and colourless \(3/2/1\) obligations, and
  the single-operator warning from adjacent mechanism notes.

## Random Source Fragments Reviewed

- Tong string notes, pages 31--50: transverse string constraints, level
  matching, and lightcone physical oscillator variables.
- Logan Higgs TASI, pages 1--10 and 31--40: Standard Model gauge-Higgs setup,
  hypercharge conventions, W/Z mass generation, and Goldstone-vector mixing.
- Acharya--Gukov, pages 51--70: \(G_2\) conical singularities, anomaly
  inflow, chiral fermions, ADE enhancement, and rank-one higher singularities.
- Martin--Robertson, pages 11--20: \(\MSbar\) heavy-particle masses,
  tadpole-free \(v\), and scheme sensitivity for W/Z/H/top quantities.
- Witten small-instanton pages 21--23: six-dimensional vector multiplet
  source memory and the role of singularities in supplying missing states.

## Consequence For Loop 42

The review of `O1OrderedSampling.lean` and Logan's Goldstone discussion
supports the Ward-projected current-insertion edit in Sec. IV.  The neutral
projector should be stated on the transverse \((W^3,B)\) trace space with the
photon unit vector removed and a hypercharge remainder carried as theorem
data.  The parent Lean notes also support keeping the CHM current entry as the
active proof spine.

## New Pending Items

- Inspect CHM rendered pages for the exact neutral transverse product used by
  \(P_Z\) and \(P_\gamma\).
- Add a future \(G_2\) local test with one ADE enhancement \(G\subset\widehat G\),
  one \(f_Q\), and one pairing.
- Revisit Martin--Robertson during the calculation phase for pole and
  Breit--Wigner convention conversion.

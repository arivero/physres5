# Cross-import from the kkFable run (user-directed, 2026-06-12)

**Verdict line: REFERENCE / cross-project import (read-only spy of
`~/kkFable/notes` and `~/kkFable/out/OPTIONB_X11_DIDACTIC.tex`, per user).
Five imports; one new ledger candidate (i); two ledger cross-references.
Nothing from kkFable is treated as derived here without its own note.**

kkFable (Option-B re-derivation run on the Aloff–Wallach space X_{1,1},
2026-06-11/12; pencil-and-paper + two codex collaborator checks) is the
carrier-side sister of this program: it asks where the de Vries sectors
LIVE on a concrete geometry; dvFable asks what FORCES the coefficients.

## 1. The two programs' open cores coincide

kkFable's verdict ledger (C7 row) keeps exactly two postulates open:
**c = 1** in the signed Hessian `A(D) = [[0, D†],[D, −c·D†D]]`, and
**D†D = j(j+1) as a production premise**. These are this program's lock
(c = 1 ⇔ r = 1 ⇔ the diagonal c₁ = c₂ ⇔ Σ_j(0) = μ²) and map (zero-mode
bare Casimir) — independently isolated by a different method on a
different object. The lock is the invariant content of the whole subject;
two programs converging on it is itself T3-relevant (it is THE one
condition, everywhere).

## 2. New ledger candidate (i): the Hessian/variational formulation

kkFable's open "Deliverable 4": construct a first-order operator D_patch
whose second variation is A(D) with c = 1. As a HESSIAN (second variation
at a critical point), the indefinite block is legitimate — saddles have
negative directions; the PSD no-go binds mass matrices, not Hessians —
so this is a structurally new attack class for the lock, never run in
dvFable (iteration 1 ran gauge-invariant Lagrangians of (p, Q, π);
this asks for a superpotential/Morse-type functional whose critical
structure forces c = 1). Admission check: (a) statable without the angle
✓; (b) kill-test in one iteration: exhibit a functional class whose
critical-point Hessian is A(D) with c forced by the structure, or show
the c-knob persists in every class (then it is the same free closure
retired in iteration 2) ✓; (c) not a rephrasing: different class
(variational geometry), shared target ✓. QUEUED as (i); the caution from
the prior runs stands — the "vector-mass branch" reading of the positive
Hessian eigenvalue is itself part of what would need deriving (a saddle
Hessian is not automatically a mass matrix).

## 3. Map evidence (feeds ledger (a))

kkFable independently records: a spin^c Dirac SQUARE on the carrier gives
`C_G − C_K + |ρ|²`, not bare `j(j+1)` — their stated reason for rejecting
the bare-Casimir map as a production premise. This corroborates iteration
3's slot-split finding from the other side: geometric operators deliver
shifted/spectral Casimirs; the bare algebraic Casimir has only the
zero-mode/current-algebra origin (the GHU asset) — or it is a postulate.

## 4. Assignment evidence on a concrete carrier (feeds ledger (g))

kkFable N5: on X_{1,1} with the SM-compatible identification (colour on
the base), the j = 1/2 slot is colour-charged in every realization, so
the de Vries EW assignment would break SU(3)_c — dead; the inverted
assignment is excluded by cos²θ_W = a₁/a_{1/2} ≈ 1.287 > 1 (the
mass-ordering argument, same as the corpus orbit note). Under the
colourless reading (their Option A) the assignment survives but the
carrier is an electroweak toy without colour. Carrier-level data point:
the assignment leg fails or floats on real geometry; "0.2231 detaches
from any observable" there. For T3: the assignment is not only underived
(our (g)) but obstructed where tested.

## 5. Angle-landscape evidence (feeds T3 accounting)

kkFable N3: on the carrier the angle is either strictly undefined (no
dynamical Y boson — rank budget 4 > 3) or, counterfactually,
`sin²θ_W = (1+t)/((1+t)+k_Y)` — a free Wilking squashing modulus. Joins
iteration 3's Manton table (rational, wrong values) in the same pattern:
geometric weak angles are rigid-but-wrong or free-moduli — never the de
Vries number. One more axis cell for the look-elsewhere table.

## 6. Style model for G3

`OPTIONB_X11_DIDACTIC.tex` is the format the user wants for the final
paper: physics-first narrative ("the toolbox in five moves"), derivations
in prose with hand-checkable steps, a scorecard section, obstructions
stated plainly, representation data in appendices — no internal run
bookkeeping. Use it as the structural template when G3 opens.

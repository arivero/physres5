# Loop 20 Referee A Report

## Central Claim

The paper presents a conditional source-to-pole program.  The algebraic matrix
\(Q(J)\) fixes the positive-root quotient, and the physical claim requires the
ordered electroweak assignment plus source-to-pole matching.

## Strongest Source-Supported Result

The electroweak scheme discipline is the strongest established part: the
Standard Model Higgs mass map supplies the projective \(W/Z\) ratio, photon
null direction, and radial electroweak ray, while pole, running, and
Breit--Wigner conventions stay separated.  The CHM interval material supplies
real boundary-kernel ingredients.

## Weakest Inference

The ordered sampling rule remains the main electroweak gap.  The Higgs
doublet and adjoint-current invariants are available source data; the map from
those data to charged and neutral pole samples is the open theorem.

## Hidden Assumptions

- One \(J\) normalization applies across Higgs, adjoint, endpoint, interval,
  and \(G_2\) data.
- A two-dimensional light space exists and extra light modes decouple or block
  diagonalize.
- The route kernel survives matching into the dressed transverse pole
  condition.
- The spectral variable \(\lambda\) has a scheme map into a pole self-energy
  variable.
- The negative branch has a gauge-invariant scalar functional.
- CHM boundary equations are schematic until dimensions, signs, variables, and
  normalization are specified.

## Exact Revisions

- Tighten VI.G so source papers support ingredients, while subspace selection,
  inner product, normalization, and DeVries entries remain theorem data.
- Change the electroweak assignment language from natural invariants to
  available invariants and state that the mass matrix leaves the ordered
  sampling map open.
- Add a route-dependent matching remainder \(\Delta^{(r)}_{\rm match}\) to
  the pole-ratio equation.
- Recast the Sec. IX filter sentence as journal prose.
- Correct the O18 Regge formula to the additive oscillator form used elsewhere.

# Loop 37 Referee A

## Central Claim

The manuscript treats the DeVries quotient as a pole-spectrum theorem target,
\[
  M_{W,\rm pole}^2/M_{Z,\rm pole}^2
  =
  x_+(3/4)/x_+(2)+\Delta ,
\]
with \(J_H=3/4\) drawn from the Higgs doublet channel and
\(J_{\rm adj}=2\) drawn from an adjoint/current channel.

## Strongest Source-Supported Result

Sec. IV gives a controlled electroweak baseline: Higgs kinetic term, tree-level
\(W/Z/\gamma\) mass matrix, custodial \(\rho=1\), and the radial
broken-to-unbroken path at fixed \(g:g'\). Appendix E is strongest when it
treats Witten's \(D=11\) endpoint and six-dimensional string settings as
source-supported arenas, with the DeVries middle line stated as a reconstruction
target.

## Weakest Inference

The ordered sampling rule \((J_W,J_Z)=(3/4,2)\) remains the weakest inference.
The text labels it open, yet the charged-pole prose can suggest that the
\(W\) sample already follows from the Higgs doublet Casimir. A referee will ask
why the charged vector pole samples \(C_2(\mathbf 2)\), while the neutral
massive vector samples \(C_2({\rm adj})\). The Higgs-current paragraph is a
useful clue and still needs the quotient derivation.

## Hidden Assumptions

- The source variable \(u_{\rm EW}\) controls projectors, inner product,
  normalization, photon zero, scalar branch, and pole map in one basis.
- Complex-pole matching commutes with the two-channel reduction up to a
  controlled \(\Delta_{\rm O1}\).
- The \(U(1)_{\rm geom}\to U(1)_{\rm em}\) endpoint requires a charge lattice
  and generator normalization.
- The top-sector extension assumes the same source variable governs the
  reduced kernel and \(\mathcal Y_{\rm top}\).

## Exact Revisions Needed

- Replace "Loop 33 makes..." in Sec. IV with manuscript prose naming the
  same-source projection package.
- Remove compiled-text references to `../prTalks`, parent-workspace material,
  project provenance, and recall prompts from Appendix E.
- Replace "active refinement" in the introduction with theorem-target language.
- Replace remaining body uses of "pending" in the route, \(G_2\), and branch
  sections with obligation language.
- Keep "loop effects" in the pole section, since it is a physics phrase.

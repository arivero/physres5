# Normalization Loop 50 Referee A

## central claim

O10 can close as a manuscript-architecture issue.  Appendix E and Appendix D
Target VI now supply the endpoint dictionary, source-status taxonomy,
single-source datum, middle-line Schur-complement target, pole-matching
requirement, and failure modes.  The derivations remain residual theorem
targets.

## strongest source-supported result

Appendix E separates the source levels correctly.  Witten supports the
\((D,n_{\rm KK})=(11,7)\) \(SU(3)\times SU(2)\times U(1)\) endpoint and
fermion obstruction.  The \(D=9\) endpoint is kept as
\(U(1)_{\rm geom}\) until an electromagnetic embedding theorem supplies the
charge lattice and kinetic normalization.  The \(D=10/6\) middle lines are
reconstruction labels.  Target VI mirrors the same pass/fail data.

## weakest inference

The open bridge is the jump from dimensional labels to the electroweak pole
quotient.  No source route has derived \(P_W\), \(P_Z\), \(P_\gamma\),
\(\Lambda_J\), \(\widehat K_J\), \(\mathcal R_{\rm pole}\), or the ordered
assignment \((J_H,J_{\rm adj})=(3/4,2)\).  Target VI must keep those steps
conditional.

## hidden assumptions

- A common source coordinate \(u\) controls \(t_{\rm dim}\), the allowed
  electroweak ray \(t_{\rm EW}\), the kernel, scalar data, and pole matching.
- Extra channels can be eliminated or block-diagonalized without changing the
  pole quotient.
- The geometric \(U(1)\) endpoint has a Standard Model embedding after a
  charge-lattice and normalization theorem.
- The two-channel basis \(\{h_J,a_J\}\) fits both the order-parameter channel
  and the adjoint/current channel.

## exact revisions needed

- Close O10 with architecture-only wording.
- Preserve residuals under O1/Target I, O3/Target III, O4, O8,
  O17/Target VII, and O20/Target X.
- Tighten Target VI language so the ordered samples follow only after the
  assignment target is completed.
- Tighten Appendix E branch language so assignments follow after the
  pass/fail kernel is obtained.

# Loop 30 Referee A Report

## Central Claim

The next normalization pass should make the CHM interval/current-entry route
the active source-controlled derivation test.  The hatted diagonal current
entry remains the target:
\[
\widehat K^{\rm cur}_J(\widehat\lambda)
=
\widehat\lambda+\widehat\Sigma_{aa,J}^{\rm CHM}
+O(\widehat\lambda^2),
\qquad
\widehat\Sigma_{aa,J}^{\rm CHM}=J.
\]

## Strongest Source-Supported Improvement

Add a CHM convention-audit insertion.  The useful source-backed ledger contains
endpoint orientation signs in the vector/scalar boundary equations, dimensions
of boundary kinetic coefficients and boundary vevs, the CHM product used to
normalize \(a_J\), the reference subtraction \(K^{\rm ref}_{T,J}\), the photon
zero-mode projection, and the distinction among the boundary eigenvalue
\(\lambda\), \(m_n^2\), and the pole variable \(s\).

## Weakest Inference

The jump from CHM boundary data to
\(\widehat\Sigma_{aa,J}^{\rm CHM}=J\) remains the weak point.  CHM supports
boundary kernels and modified products.  The DeVries normalization, the
invariant \(J\), and the common \(\Lambda_J\) remain theorem data.

## Hidden Assumptions

1. One source scale \(\Lambda_J\) applies to \(J_H=3/4\) and
   \(J_{\rm adj}=2\).
2. The transverse boundary-current sample
   \(a_J=P_{a,J}A_\mu^{T,\partial}\) is normalized by the CHM product.
3. \(K^{\rm ref}_{T,J}\) is fixed by a photon channel or boundary subtraction.
4. Extra interval modes decouple or block-diagonalize before the two-channel
   determinant is formed.
5. Gauge fixing separates physical scalar and eaten-Goldstone data while
   preserving the transverse current entry.
6. The CHM boundary eigenvalue maps to the dressed transverse pole self-energy
   with a controlled \(\Delta^{(\mathrm{int})}_{\rm match}\).
7. The charged W comparison samples the Higgs/order-parameter invariant through
   a derived boundary coupling.

## Exact Edit Targets

- `manuscript/sections/06c_kaluza_klein_boundary.tex`: add a CHM
  convention-audit paragraph and expand the hatted current-kernel definition.
- `manuscript/sections/06g_route_comparison.tex`: state the source/theorem
  split and make the CHM current entry the active pass/fail route test.
- `manuscript/sections/D_theorem_targets.tex`: add the acceptance condition
  for the rendered-PDF convention audit.
- `OPEN_ISSUES.md`: update O4 with the Loop 30 current-entry refinement.

## Score

5/5 for a source-control normalization pass.  No issue should close.

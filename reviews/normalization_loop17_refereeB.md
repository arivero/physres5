# Loop 17 Referee B: String, Kaluza--Klein, and Source Control

## Central Claim

O18 belongs in the theorem-target appendix.  A Regge completion can promote
DeVries roots to sector intercepts, while particle identity and experimental
channels require a source-backed assignment theorem.

## Strongest Source-Supported Result

The manuscript already has a strong structural route: endpoint, interval, KK,
and \(G_2\) mechanisms are arenas for a two-channel determinant.  The Regge
source fragments supply the common-slope tower template; the DeVries kernel
supplies the candidate intercept datum after it is derived from a source
operator.

## Weakest Inference

The \(j=3/2\) slot as a specific particle is unsupported by the current local
source inventory.  Adjacent `prTalks`, `physres6`, and signed-root notes are
project provenance.  Primary Regge/string sources, experimental sources, and a
collider ledger are required for journal-facing phenomenology.

## Hidden Assumptions

- \(J=s(s+1)\) in the DeVries equation and \(J_{\rm phys}\) in a Regge
  trajectory are separate symbols.
- The positive root is an intercept datum; oscillator or KK level supplies
  large-spin growth.
- The negative branch needs a stability, projection, or partner-channel rule.
- Open-string endpoint data and compact winding data need distinct source
  interpretations.

## Required Revisions

Add the tower
\[
M^2_{N_{\rm osc},j,\sigma}
=\mu^2x_\sigma(j(j+1))+\frac{N_{\rm osc}}{\alpha'}
+\Delta^{\rm Regge}_{N_{\rm osc},j,\sigma}
\]
as a theorem target.  Add a failure mode: absent the source kernel, common
slope, and projection rule, higher slots remain algebraic branch labels.

# Prompt: analytical calculation agent

Solve the assigned analytical gap. Start from the equations already in the repository. State assumptions. Derive the result symbolically. If a derivation fails, produce the strongest conditional statement and identify the missing assumption. Update both the relevant `calculations/*.md` file and the manuscript section. Run `make test` and `make manuscript`.

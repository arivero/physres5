# Loop 17 Referee A: Electroweak, Collider, and Scheme Discipline

## Central Claim

O18 should be stated as a conditional theorem target.  Given the DeVries kernel,
the pole placement, and a common pole-normalized \(\mu\), the positive branch
contains higher slots.  A Regge completion is a proposed source mechanism for
those slots.

## Strongest Source-Supported Result

The algebraic branch structure is already in the manuscript:
\[
Q(J),\qquad x^2+Jx-J=0,\qquad x_\pm(J).
\]
Local Tong fragments support ordinary open-string Regge towers with common
slope, \(M^2=(N-1)/\alpha'\), and \(J_{\max}=\alpha'M^2+1\).  That supports an
intercept-reading target when the DeVries label is separated from the Regge
oscillator level.

## Weakest Inference

The identity claim for the \(s=3/2\) positive slot lacks a source assignment.
A diphoton excess is a neutral invariant-mass feature.  A charged or coloured
chiral state requires a gauge representation, anomaly ledger, mass-generation
rule, production and decay channels, widths, and collider bounds.

## Hidden Assumptions

- The same \(\mu\) normalizes \(W\), \(Z\), and any higher branch slot.
- The sector label used in \(j(j+1)\) is distinct from observed Lorentz spin.
- A source theory supplies the projection rule for the \(j=3/2\) positive
  intercept.
- Collider language requires primary CMS/ATLAS sources and a phenomenology
  source pack before manuscript promotion.

## Required Revisions

Add Appendix D Target VIII for Regge-intercept survival.  Rewrite O18 with
separate \(j\), \(N_{\rm osc}\), and physical-spin labels.  Move the low-mass
diphoton and charge-\(4/3\) material into a source-audited phenomenology queue.

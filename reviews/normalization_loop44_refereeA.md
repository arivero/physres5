# Loop 44 Referee A

Role: skeptical PRD referee focused on electroweak correctness, source-product
discipline, exact-looking theorem targets, and manuscript prose.

Files edited by referee: zero. Numerical checks: zero.

## Central Claim

The manuscript frames the DeVries quotient as a conditional pole-spectrum
theorem target,
\[
\frac{M_{W,\rm pole}^2}{M_{Z,\rm pole}^2}
=\frac{x_+(3/4)}{x_+(2)}+\Delta_{\rm O1},
\]
with \(J_H=3/4\) assigned to the Higgs/order-parameter channel and
\(J_{\rm adj}=2\) assigned to the photon-orthogonal neutral current channel.

## Strongest Point

Section IV gives the cleanest source-supported core: the electroweak mass
matrix, photon null direction, fixed \(g:g'\) ray, and Goldstone-vector mixing
test.  The CHM source trail is also disciplined: boundary kinetic products,
vector/scalar boundary equations, photon/custodial bookkeeping, and DeVries
normalization data are separated.

## Weakest Point

The source-normalized photon and \(Z\) trace definitions reduce risk, since
\(\Delta_Y\) remains explicit and the photon direction is normalized in the
source product.  The remaining risk is the theorem status of \(P_{a,J}\),
\(\Pi_{\gamma^\perp}^{(T)}\), and the source product.  A CHM construction must
prove that the projected photon trace, projected \(z^{(0)}\), and current entry
live in one positive neutral space.

## Hidden Assumptions

- \(P_{a,J}a_\gamma^{(0)}\) and
  \(P_{a,J}\Pi_{\gamma^\perp}^{(T)}z^{(0)}\) have nonzero source norm.
- \(P_{a,J}\) is compatible with the photon-orthogonal projector, or the
  projection order has a derived meaning.
- The CHM product for neutral traces also supports the charged
  order-parameter sample.
- Photon-reference subtraction implements the Ward identity after matching to
  the four-dimensional pole scheme.
- One \(\Lambda_{\rm CHM}\) normalizes \(J_H=3/4\) and \(J_{\rm adj}=2\).

## Exact Revisions

- Add Sec. IV admissibility conditions: nonzero denominators, positive source
  product on the projected traces, and projection-order data.
- Align Sec. VI.C notation with the Loop43 source-normalized
  \(a_J^{\gamma^\perp}\) construction.
- Add \(\Delta_W^{\rm CHM}\) and \(\Delta_Y^{\rm CHM}\) to the Sec. VI.C
  closure list.
- Keep Appendix D/E compression queued after the CHM projector package is
  tighter.

## Highest-Value Loop 44 Target

Unify Sec. IV and Sec. VI.C around one CHM-normalized projector package:
\[
(a_\gamma,a_J^{\gamma^\perp},h_J;\,
P_\gamma,P_Z,P_W;\,\Delta_Y,\Delta_W,\Delta_{\rm O1})
\]
with source product, nonzero norms, projection order,
\(\Lambda_{\rm CHM}\), and photon-reference subtraction stated together.

## Scores

Conceptual clarity 4; source control 4; electroweak correctness 4; proof-spine
quality 3; PRD prose 3; open-gap honesty 5.

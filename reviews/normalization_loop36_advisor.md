# Normalization Loop 36 Advisor

## one new conceptual route

Treat O18/O19 as one sector-duality target.  The label \(j\) names a reduced
boundary sector whose eigenbranches are paired by the involution
\(x\mapsto -J/x\).  The high-risk route tests whether the positive branch is a
bounded endpoint/intercept datum while the negative branch is the dual
boundary-condition sector carrying scalar or modulus data.

## one string/D-brane mechanism to test

Build a two-boundary Chan--Paton reduction where T-duality exchanges
all-Dirichlet and all-Neumann boundary conditions, then ask whether the
reduced quadratic kernel has
\[
K_j\sim
\begin{pmatrix}
0 & \sqrt{j(j+1)}\\
\sqrt{j(j+1)} & -j(j+1)
\end{pmatrix}.
\]
The test must include boundary conditions, charges, tensions, projection rule,
and negative-branch reading.

## one source to read

A primary source for the rotating \(p\)-brane Regge scaling law
\(M\sim s^{p/(p+1)}\).  Target IX currently has Tong support for
D-brane/T-duality vocabulary, while the exponent itself is source-open.

## one equation or diagram the manuscript needs

Add a commutative diagram tying Target VIII to Target IX:
\[
\mathcal S_{\rm open/D}
\to (K_j,\alpha',P_{\rm surv})
\to M^2_{N,j,\sigma}
\to
\begin{cases}
j=3/2\ \text{assignment ledger},\\
x_+x_-=-j(j+1)\ \text{branch-duality test}.
\end{cases}
\]
This states that higher-slot phenomenology and branch-scaling admissibility
are outputs of the same source package.

## implementation advice

Keep O18 phenomenology quarantined pending projection and gauge quantum
numbers.  For Loop 36, add one paragraph that names the shared source package
\((K_j,\alpha',P_{\rm surv},\mathcal B_{\rm ND},\mathcal Q,\mathcal T)\), then
state that O19 fails if the involution cannot be lifted from algebra to
boundary-condition duality.

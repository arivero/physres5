# Normalization loop 12 - Referee A

## Central claim

Appendix D frames the manuscript as a conditional derivation problem.  The
DeVries quadratic is algebraically fixed, and the positive-root quotient at
\((J_H,J_{\rm adj})=(3/4,2)\) is proposed as a W/Z complex-pole spectrum
datum.  PRD-level status depends on deriving the ordered electroweak
assignment, the two-channel source kernel, and the pole-matching chain in one
scheme.

## Strongest source-supported result

Target I gives the strongest source-controlled material: complex poles,
Breit--Wigner/on-shell conventions, running angles, and high-scale boundary
data are kept distinct, with an explicit Breit--Wigner-to-pole map before
forming \(\sin^2\theta_{\rm pole}\).  The electroweak background facts are
also stable: \(H\) has \(C_2=3/4\), the \(SU(2)_L\) adjoint has \(C_2=2\), and
the tree-level mass matrix gives the usual W/Z masses.

## Weakest inference

Target II remains the weakest inference.  The charged W pole must receive the
Higgs-doublet invariant through a derived mathematical channel, while W and Z
are both vector poles after symmetry breaking.  Target VI inherits this
dependence because its middle-dimensional sector also has to produce the same
ordered samples.

## Hidden assumptions

- One inner product and normalization apply to \(J=3/4\) and \(J=2\).
- Extra light modes decouple or block-diagonalize.
- The determinant survives EFT matching, projection, field redefinition, and
  pole renormalization in a controlled way.
- Source-kernel labels become the same \(SU(2)_L\) invariants used in the
  electroweak mass map.
- The negative branch has gauge-invariant scalar meaning.
- Local \(G_2\), interval, and endpoint routes can be compared using a common
  reduced basis.

## Exact revisions needed

- Target I should state a theorem target for pole-spectrum identification.
- Target II should define the mass-map theorem and list the allowed channels
  for \(J_H=3/4\) in the charged comparison.
- Target III should require field basis, inner product, projection rule, and
  normalization.
- Target VI should make its ordered-sampling statement dependent on Target II.
- The acceptance criterion should include the pole-matching arrow.

## OPEN_ISSUES updates

- O1 should point to Target II as a mass-map theorem.
- O4 should require reduced basis, inner product, projection or decoupling rule,
  and normalization before comparing \(\Sigma\)-entries.
- O8 should record determinant preservation or reconstruction under matching,
  plus the \(G_2\) compactification-to-EFT-to-pole issue.

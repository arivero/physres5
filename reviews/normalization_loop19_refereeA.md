# Loop 19 Referee A Report

## Central Claim

The interval/Kaluza--Klein route is a well-posed derivation target.  The draft
uses Csaki--Hubisz--Meade as source support for variational boundary
conditions, brane kinetic terms, boundary scalar vevs, vector spectra, and
scalar remnants.  The DeVries-specific entries
\[
  \Sigma_{hh}=0,\qquad \Sigma_{aa}=J,\qquad
  \Sigma_{ha}\Sigma_{ah}=J
\]
remain the target.

## Strongest Source-Supported Result

Boundary localized terms change the spectral problem.  The CHM fragments give
eigenvalue-dependent boundary conditions, a modified scalar product, vector
Robin data from boundary scalar vevs, and possible \(A_5/\pi_i\) scalar
remnants.  This supports a real interval secular-equation mechanism.

## Weakest Inference

The ordered electroweak assignment \((J_W,J_Z)=(3/4,2)\) requires the exact
boundary action, charged and neutral mode equations, photon zero mode,
custodial protection, and pole matching.

## Hidden Assumptions

- The two-dimensional light sector survives the full interval reduction.
- Extra \(A_5\), boundary Goldstone, and KK modes decouple or block diagonalize.
- The source normalization yields one zero diagonal entry, one \(J\) diagonal
  entry, and off-diagonal product \(J\).
- The same \(J\) controls the diagonal and mixing data through one source
  operator and one inner product.
- The negative branch maps to a gauge-invariant scalar, boundary, or modulus
  functional.

## Exact Revisions

- Add the source-derived interval data
  \[
    \partial_y f=-(m_n^2/M)f,\qquad
    (f,g)=\int fg+\frac1M f(0)g(0),\qquad
    \partial_y A_\mu\mp v_i^2A_\mu=0.
  \]
- Separate the CHM data leading to an interval kernel from the open DeVries
  entry equality.
- Rename the interval dictionary source column so it reads as candidate source
  data.
- Add the electroweak interval proof chain through
  \(\{\mathcal B_W,\mathcal B_Z,\mathcal B_\gamma\}\), photon masslessness,
  and the W/Z pole ratio.
- Convert the projective electroweak sentence into a theorem target.

# Loop 44 Referee B

Role: independent PRD referee focused on string, Kaluza--Klein, \(G_2\),
source control, and structure.

Files edited by referee: zero. Numerical checks: zero.

## Central Claim

The manuscript presents the DeVries relation as a conditional pole-spectrum
program.  One endpoint, interval KK/CHM, \(G_2\), or superconnection source
must derive the two-channel kernel, ordered \(W/Z\) assignment, pole-matching
remainder, and negative-branch scalar map from one normalized source package.

## Strongest Point

The interval/CHM route remains the controlled part.  CHM supplies boundary
kinetic terms, modified products, vector Robin data, scalar \(A_5/\pi_i\)
equations, photon-zero bookkeeping, and custodial boundary structure.

## Weakest Point

The route-comparison proof obligations identify a boundary-kernel arena.  A
source-derived DeVries matrix entry is still absent.  The equality
\(\widehat\Sigma_{aa,J}^{\rm CHM}=J\) remains a theorem target.

## Hidden Assumptions

- A single source product can normalize \(h_J\), \(a_J^{\gamma^\perp}\), and
  \(a_\gamma\).
- The charged \(W\) sample can inherit \(J_H=3/4\) through Goldstone-vector or
  boundary-Higgs data.
- The CHM boundary determinant can survive matching into a four-dimensional
  complex-pole statement.
- The \(G_2\) Higgs-bundle/Morse data can be reduced to a two-state kernel with
  a stated pairing.
- Appendix E's dimensional labels correspond to a source family \(u\).

## Exact Revisions

- In Sec. VI.G, state that source equations fix the candidate entry order.
- Add a pass/fail ordering: first derive
  \(\widehat\Sigma_{aa,2}^{\rm CHM}=2\); second test
  \(\Sigma_{ha}\Sigma_{ah}=J\); third attach \(P_W,P_Z,P_\gamma\) and
  \(\Delta_{\rm O1/O10}\).
- Later, sharpen the \(G_2\) mechanism by choosing one ADE adjoint fluctuation
  with a local pairing or one Higgs-bundle flow overlap.
- Keep Appendix D/E compression queued after the CHM current-entry subtarget is
  tighter.

## Highest-Value Loop 44 Target

Derive or reject the CHM current entry
\[
\widehat\Sigma_{aa,2}^{\rm CHM}=2
\]
from source product, photon subtraction, \(P_{a,2}^{\gamma^\perp}\),
\(\Lambda_{\rm CHM}\), endpoint signs, and pole map.  The practical edit is the
same CHM-normalized projector package identified by Referee A.

## Scores

Conceptual clarity 4; source control 4; string/KK/\(G_2\) depth 4; proof-spine
quality 3; PRD prose 3; open-gap honesty 5.

# Loop 28 Referee A Report

## Highest-impact issue

Work O4 through the CHM interval current-entry subtarget.  This is the
source-controlled route closest to one DeVries matrix entry:
\[
K^{\rm cur}_J(\lambda)
=
\langle a_J,(K^{\rm DtN}_{T,J}+K^{\rm brane}_{T,J}
-K^{\rm ref}_{T,J})a_J\rangle_{\rm CHM},
\qquad
\Sigma^{\rm CHM}_{aa,J}=J .
\]
It also feeds O1, O8, and O3 through the same field basis, projection, photon
zero mode, and pole map.

## Recommended edits

- In Sec. VI.C, add convention data after the current-entry target:
  \(\Sigma^{\rm CHM}_{aa,J}\equiv K^{\rm cur}_J(0)\) is meaningful after
  \(K^{\rm ref}_{T,J}\), endpoint-normal signs, brane dimensions, and the CHM
  product normalization are fixed from the CHM action.
- In Sec. VI.G, identify the source-led task as deriving or rejecting the CHM
  diagonal current entry \(\Sigma^{\rm CHM}_{aa,J}=J\), while the off-diagonal
  square-root entry and scalar branch stay as linked theorem targets.
- In Appendix D Target III, give the current-entry theorem with a declared
  subtraction convention.
- In `context/source_inventory.md`, sharpen the CHM fragment anchors:
  pages 1--10 for variational boundary conditions and modified product,
  pages 11--20 for gauge fixing, vector Robin data, and \(A_5/\pi_i\)
  equations, pages 21--30 for photon/custodial/electroweak boundary data.
- In `context/concept_claims_matrix.md`, split CHM source facts from DeVries
  theorem data.

## Issue to record

Add an O4 source-control refinement: before equation-level use of
\(K^{\rm cur}_J\), verify from the rendered CHM PDF the endpoint signs,
dimensions of boundary kinetic terms, scalar-product normalization, and the
precise variables entering \(K^{\rm brane}_{T,J}\).

## Score

Normalization progress: 4/5.  The manuscript has journal-style theorem targets
and a source-led active route.  The missing item is a fully pinned CHM
convention ledger from source action to DeVries normalization.

# Loop 17 Advisor: Regge-Intercept Survival Route

## New Conceptual Route

Turn O18 into a Regge-intercept survival theorem.  The source theory
\(\mathcal S\) produces common-slope towers labeled by a DeVries sector \(j\).
The low intercepts are the eigenvalues of the two-channel kernel
\[
K_j(\lambda)=
\begin{pmatrix}
\lambda&-\sqrt{j(j+1)}\\
-\sqrt{j(j+1)}&\lambda+j(j+1)
\end{pmatrix}.
\]
If the positive \(N_{\rm osc}=0\) intercept survives the projection and the
ordered electroweak map fixes \(j=\tfrac12,1\), the same rule defines a
\(j=\tfrac32\) positive intercept.  Its particle identity is an additional
assignment theorem.

## Mechanism To Test

Test a momentum/current plus winding/order-parameter Schur complement:
\[
K^{\rm eff}_j(\lambda)
=K^{\rm bdry}_j(\lambda)-V_j^\dagger(\lambda-L_j)^{-1}V_j
\longrightarrow
K_j(\lambda).
\]
This connects the KK two-channel analogy, the interval Schur target, and the
Regge-intercept reading.

## Source To Read

Use the local Tong string fragment around `pages_061-070.md` for the
common-slope Regge anchor \(M^2=(N-1)/\alpha'\) and
\(J_{\max}=\alpha'M^2+1\).  Use adjacent `prTalks` and `physres6` only as
project source notes until primary sources are added.

## Equation Needed

Add the chain
\[
\mathcal S_{\rm string/KK}\to K_j(\lambda)\to
\{x_\pm(j(j+1))\}\to
M^2_{N_{\rm osc},j,\pm}
=\mu^2x_\pm(j(j+1))+\frac{N_{\rm osc}}{\alpha'}
+\Delta^{\rm Regge}_{N_{\rm osc},j,\pm}.
\]
The status is conjectural through the first arrow and algebraic after the
kernel is assumed.

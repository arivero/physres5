# Loop 33 Referee B

Role: independent PRD referee focused on string/Kaluza--Klein/\(G_2\) depth,
source control, and manuscript structure.

## Central claim

O1 claims that the W/Z pole quotient samples the ordered pair
\[
(J_W,J_Z)=(J_H,J_{\rm adj})=(3/4,2),
\]
where \(J_H=C_2(\mathbf 2)\) belongs to the Higgs/order-parameter channel and
\(J_{\rm adj}=C_2({\rm adj})\) belongs to the \(SU(2)_L\) current/gauge
channel.

## Strongest source-supported mechanism address

The CHM interval route is the best available mechanism address.  It supplies
source-controlled ingredients: boundary kinetic terms, modified scalar product,
vector Robin data from boundary scalar vevs, scalar \(A_5/\pi_i\) boundary
equations, photon zero-mode bookkeeping, and custodial boundary structure.  The
manuscript already translates these into a current-entry target
\[
\widehat\Sigma_{aa,J}^{\rm CHM}=J
\]
after photon-reference subtraction and shared \(\Lambda_{\rm CHM}\)
normalization.

## Weakest mechanism inference

The ordered step remains open: charged \(W\) sampling \(J_H=3/4\) and neutral
\(Z\) sampling \(J_{\rm adj}=2\).  Existing sources support the Higgs doublet
invariant and the adjoint-current invariant.  The manuscript still needs
\(P_W\), \(P_Z\), a shared inner product, and the pole-chain remainder
\(\Delta_{\rm match}\).  The \(G_2\) route supplies localization and
completion language until a local operator, projection, and normalization are
derived.

## Hidden assumptions

- The Higgs/order-parameter channel and adjoint/current channel live in one
  reduced two-state Hilbert space \(\mathcal H_J\).
- One dimensionless normalization applies to \(J_H=3/4\) and
  \(J_{\rm adj}=2\).
- The photon-reference subtraction fixes a physical zero for the neutral
  current entry.
- Extra interval, endpoint, and localized modes decouple or block-diagonalize
  before the DeVries determinant is read.
- The source eigenvalue \(\lambda\) maps into a four-dimensional transverse
  pole condition in the pole scheme used for \(M_{W,\rm pole}\) and
  \(M_{Z,\rm pole}\).
- The same source operator accounts for the negative branch.

## Required revisions

Promote the CHM current-entry pass to a strict ledger defining \(P_{a,J}\),
\(P_\gamma\), \(K^{\rm ref}_{T,J}\), \(\Lambda_{\rm CHM}\), endpoint signs,
and the pole map in one table.  State which CHM source equation fixes each
normalization datum.  Add a parallel row for the charged/order-parameter sample
\(h_J\), with the same \(\Lambda_{\rm CHM}\) and inner product.  Keep the
superconnection/Hodge route as a grading and off-diagonal test until it
supplies \(P_W\), \(P_Z\), \(B_J\), and \(\Delta_{\rm sc}\) from one reduction.
Treat \(G_2\) as a localization and completion check until it derives a
normalized local kernel \(K_J^{G_2}\) and source-to-pole matching.

## Scores

- Theorem target: 4/5.
- Derived mechanism claim: 1/5.


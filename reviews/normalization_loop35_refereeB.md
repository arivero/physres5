# Normalization Loop 35 Referee B

## central claim

O17 can enter the manuscript as a conditional endpoint theorem.  A source
route that supplies \(U(1)_{\rm em}\), scalar/vector normalization, and
pole/running matching would give
\[
\alpha_{\mathcal S}(Q_\alpha)
=
\frac{g_{\rm sec}^2\sin^2\theta_{dV}}{4\pi}
+\Delta_{\alpha,{\rm match}} .
\]

## strongest source-supported result

The manuscript has good source control for the ingredients.  Witten supports
Kaluza--Klein gauge-coupling dependence on compact metric data.  CHM supports
interval boundary kernels and modified inner products.  Jegerlehner supports
running-\(\alpha\) scheme discipline.  The six-dimensional string sources
support a constrained arena for the colourless \(D=6\) middle line.

## weakest inference

The main gap is the conversion of \(U(1)_{\rm geom}\) into Standard Model
\(U(1)_{\rm em}\) with charge lattice, generator normalization, and
\(e_{\rm geom}\).  The second gap is the scalar assignment
\(J_v=2,\ C_v=2\), which supplies the symbolic alpha route pending the
negative-branch vacuum theorem.

## hidden assumptions

- A single source variable controls \(t_{\rm dim}\), \(t_{\rm EW}\), \(v\),
  \(\Lambda_J\), \(P_J\), \(\widehat K_J\), and \(\mathcal R_{\rm pole}\).
- The photon zero mode survives the endpoint reduction with Standard Model
  charges.
- The compactification or interval normalization fixes \(Q_\alpha\).
- Six-dimensional string or \(G_2\) language supplies an actual reduced
  operator.

## exact revisions needed

- Add a compact Target VII dependency table listing outputs by route:
  endpoint, interval/CHM, and \(G_2\).
- In Appendix E, expand Eq.~\(\eqref{eq:em-endpoint-kinetic-normalization}\)
  into a pass/fail item with \(\xi_{\rm em}\), charge lattice, \(g_D\),
  volume metric, and threshold map.
- In Sec. VI.G, state that O17 follows the current-entry theorem,
  the ordered W/Z map, and the negative-branch scalar normalization.
- Keep alpha language in theorem-target form pending \(Q_\alpha\) and
  \(\Delta_{\alpha,{\rm match}}\) from source data.

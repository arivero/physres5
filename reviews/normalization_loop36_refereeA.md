# Normalization Loop 36 Referee A

## central claim

O18 and O19 frame higher DeVries roots as conditional string/brane theorem
targets: \(j\) labels a DeVries sector, \(N_{\rm osc}\) labels the
Regge/KK tower, and branch scaling tests a brane interpretation after a source
theory derives the kernel and branch map.

## strongest source-supported result

The label separation in Target VIII is PRD-safe.  Tong and Polchinski support
ordinary open-string Regge towers, Chan--Paton matrix data, D-brane gauge
fields, adjoint scalars, and T-duality boundary-condition vocabulary.  The
manuscript treats \(\{x_\pm(A_j)\}\), the \(j=3/2\) slot, and the
point/space-filling reading as theorem targets with explicit missing
derivations.

## weakest inference

Target IX uses \(s\) as a rotating-brane spin variable while Target VIII says
that \(j\) is a sector/intercept label and physical spin comes from the source
theory.  The algebraic asymptotics are controlled, while the step from
large \(J=s(s+1)\) to D0/space-filling behavior requires a proof that the
DeVries sector label is the angular momentum entering the rotating-brane law.
The negative branch also needs a mass or scalar-functional reading.

## hidden assumptions

- A common \(\mu\) applies across electroweak and higher sectors.
- \(\Delta^{\rm Regge}_{N_{\rm osc},j,\sigma}\) preserves a common slope and
  keeps the DeVries signal visible.
- The projection/BRST/GSO rule leaves the relevant positive branch alive and
  handles negative \(x_-\) consistently.
- The branch involution \(x\mapsto -J/x\) can be realized by
  boundary-condition duality.
- A neutral \(95.4\) GeV diphoton ledger is phenomenology context pending
  gauge representation, production, decay, width, and exclusions.

## exact revisions needed

- In Target IX and O19, rename the brane-scaling variable to putative
  rotating-brane angular momentum and state that identifying it with the
  DeVries sector label is an additional hypothesis.
- Add a sentence after the scaling table that the D0/space-filling labels are
  admissibility diagnostics pending the rotating-brane exponent source and
  boundary-condition map.
- In Sec. VI.B, qualify the shared sector label with this hypothesis.
- In Target VIII, confine the Biekotter citation to collider-ledger prose.

# Prompt: revision integrator

Merge the results of multiple Codex tasks. Resolve duplicate prose. Preserve equations and citations. Move repeated context into the introduction or appendices. Check that `OPEN_ISSUES.md` reflects the current state. Run `make manuscript` and `make test`.

# Normalization loop 10 - Referee B

## Central finding

The parent workspace strengthens this loop as a normalization and exclusion
audit.  The strongest import comes from `../weak`: the two-channel form is easy
to write and hard to derive from a source operator.

## Main technical guardrails

- `../weak/PHASE_1.md` shows \(a=1\) can be treated as a scale convention,
  while \(b=1\) remains an independent postulate unless a source operator fixes
  it.
- `../weak/LAGRANGIAN.md` shows the Hosotani/Wilson-line route gives
  weight-squared spectra.  A DeVries route must instead supply the full
  \(C_2(R)\) block and the off-diagonal product.
- `../weak/LAGRANGIAN_PC.md` shows the Wigner-Eckart escape route fails for
  the natural parent examples tested there.
- O4 should say every endpoint, interval, KK, brane, or \(G_2\) derivation must
  produce \(\Sigma_{aa}=J\) and
  \(\Sigma_{ha}\Sigma_{ah}=J\) from one source operator and one normalization
  rule.

## Electroweak and flavor boundary

For O1, the SO(32) warning in `../weak/PHASE_5.md` is useful: the SO(32)
adjoint supplies triplets, while the weak doublet lives in a spinor branch.
The singlet-discarding rule is external.  SO(32) flavor bookkeeping therefore
belongs in O6; carrying the ordered electroweak assignment requires a coupling
to the electroweak operator.

For O10/source notes, `../phys3` is useful as project provenance.  The
`unbroken_susy.md` interpolation note, the bootstrap charge counts, and the
SO(32) projection notes should remain project-source trails with primary
source upgrades.

## Negative branch

Import the caution only.  Parent files record negative-slot/top/Higgs
proximities, while the signed-root audits classify negative roots as branch
objects requiring a scalar functional.  The current manuscript should treat
the negative-branch/top/Higgs question as an obligation.

## Exclusions

`../dualsm` is workflow and duality-status discipline.  `../recap` is mostly
Koide and fermion-mass material.  `../orbits` is outside the current
electroweak string/KK task.  The signed-root project is useful as
negative-branch and string-UV caution because its target spurion formula
differs from the present manuscript target.

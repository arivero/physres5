# Loop 40 Open-Issue Completion Audit

Objective audited: normalize the manuscript according to
`reviews/paper_normalization_plan.md` and `reviews/referee_advisor_cycle.md`
while actively working `OPEN_ISSUES.md`.

## Requirements Used

- Main-body prose should present definitions, assumptions, constructions,
  consequences, and theorem targets.
- Process material should live in review notes, issue ledgers, Lean notes, or
  appendices.
- Each substantial loop requires two GPT-5.5 referees, one GPT-5.5 advisor, a
  fast editor, recall, compile, scans, and commit.
- Open issues should move toward closure through explicit source packages,
  equations, and success criteria.

## Loop 40 Status

Loop 40 actively works O1, O4, O10, and O20 by combining the CHM interval route
and the Coquereaux electroweak-superconnection route into one pass/fail package:
\[
\widehat K_J^{\rm sc/int}
=
\Lambda_J^{-2}P_{\rm sc}^\dagger
\left[
K_T^{\rm DtN}+K_T^{\rm brane}
+\langle\mathcal F_{\rm sc},\mathcal F_{\rm sc}\rangle_{\rm sc}
-K_\gamma^{\rm ref}
\right]P_{\rm sc}.
\]
The closure conditions are the ordered projectors, photon null projection,
extra-channel decoupling, scalar branch, and complex-pole remainder:
\[
P_W(\Phi_{\rm odd})=\frac34,\qquad
P_Z(F_{\rm even}^{\gamma^\perp})=2,\qquad
P_\gamma(F_{\rm even}^{\gamma})=0.
\]

## Issues Worked

O1 now has a single sc/int closure package for ordered W/Z sampling.  O4 now
has a single hatted sc/int kernel target.  O10 now has a concrete middle-line
source datum \(u_{\rm sc/int}\).  O20 now has an interval implementation of the
finite superconnection projection.  O3 and O8 are linked through the scalar
functional and pole remainder inside the same package.

## New Issue Noticed

The detailed IJMP A superconnection paper remains unavailable locally.  It is
needed to test whether Coquereaux scalar-product freedoms and curvature terms
can fix the finite projection and normalization in the sc/int package.  This is
recorded as a source-acquisition task in `context/source_inventory.md` and
`reviews/pending_future_work.md`.

## Completion State

The full goal remains open.  The loop advances the closure package while the
open issues remain short of their stated success criteria.  The next closure
step is equation-level testing of
\(\widehat\Sigma_{aa,2}^{\rm CHM}=2\),
\(\widehat{\mathcal I}_{h,3/4}^{\rm CHM}=3/4\), and
\(\Delta_{\rm O1/O10}\) inside the sc/int source package.

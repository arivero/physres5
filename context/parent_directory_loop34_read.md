# Loop 34 Parent-Directory Read

Date: 2026-05-22.

This note records the current read of `..` during the O4 source-kernel loop.
Credential, runtime, cache, editor, package, and generated-log paths are
inventory only.  The research imports below are project-source provenance,
guardrails, theorem targets, and source-upgrade queues.  Journal-facing physics
claims still require primary sources from `references/pdfs/` or a new audited
source entry.

## Inventory Result

Relevant local clusters remain:

| Path | Current import |
|---|---|
| `../prTalks/` | seven PDF source-note objects for electroweak ray, string/KK routes, Regge alternatives, dimensional interpolation, and top/negative-branch provenance |
| `../weak/` | electroweak mass-map critiques, Lagrangian failure modes, Wigner--Eckart failure mode, and source-kernel guardrails |
| `../physres6/` | sibling short-note route for KK fixing, branch duality, alpha/O17, and higher-slot/O18 provenance |
| `../phys4/notes/` | PhysicsForums/Wrong Turn catalogue, brane/string dictionary, top-as-brane-mode provenance, SO(32)/Seiberg-duality genealogy |
| `../signed-dv-custodial-project/` | signed-root negative-sector caution, mechanism no-go templates, earlier referee discipline |
| `../phys3/` | dimensional-interpolation and SO(32)/Chan--Paton provenance already indexed in earlier loops |
| `../dualsm/` | future duality source queue and review discipline |

Out-of-scope queues such as `../balance`, `../cantor`, `../sakurai`,
`../pdghist`, `../vixra*`, and root-level standalone TeX files remain available
for future focused issues.

## `../prTalks` Source Objects

The seven PDFs were inventoried again:

| PDF | Pages | Current status |
|---|---:|---|
| `DeVries String - Electroweak Vacuum Concerns.pdf` | 9 | electroweak ray and pole-ratio provenance |
| `DeVries String - Max Juice Ideas.pdf` | 5 | normal-form and ordered-invariant provenance |
| `DeVries String - Prompt Execution Request.pdf` | 100 | long completion prompt for string/KK operator derivation |
| `DeVries String - String theory and orbits.pdf` | 3 | orbit quadratic provenance |
| `Regge scaling of mass with spin in superstrings - Claude.pdf` | 11 | Regge/branch-scaling provenance with visual plots |
| `Scaling of m with J.pdf` | 35 | Regge-intercept and derivation-prompt provenance |
| `cosas alex - String Theory Connections.pdf` | 51 | two-channel block, representation-channel reading, and effective-dimension provenance |

Maintained access fragments remain in `context/source_fragments/parent_prtalks/`.
`pdftotext` is an access aid.  The PDFs remain the source objects for formula
layout, image-bearing pages, diagrams, radicals, and plots.

## Imports For O4

### CHM/weak guardrail

`../weak/LAGRANGIAN.md`, `../weak/PHASE_1.md`, and
`../weak/LAGRANGIAN_PC.md` reinforce the same O4 burden:
\[
\Sigma_{hh,J}=0,\qquad
\Sigma_{aa,J}=J,\qquad
\Sigma_{ha,J}\Sigma_{ah,J}=J
\]
must come from one source operator, field basis, inner product, and
normalization.  The recorded failure modes are:

- Hosotani/Wilson-line spectra naturally produce weight-squared towers and
  leave the DeVries two-channel block open.
- Single-generator spurion routes produce representation-dependent reduced
  matrix elements.
- Wigner--Eckart tests fail for the natural doublet and triplet parents as a
  source of \(\sqrt{C_2}\).
- Free Wilson coefficients reproduce the matrix shape only after the source
  theorem supplies a coefficient-locking rule.

This supports the Loop 34 choice: isolate the CHM current entry as the first
pass/fail source theorem, with the Hodge/SUSY-QM square-root entry as the
companion theorem.

### KK fixing and branch duality

`../physres6/calculations/kk_fixing_concept.md` supplies a compact conceptual
package for O3/O4/O10/O17.  The sibling note frames the negative branch as a
candidate modulus-fixing/order-parameter branch, and the positive branch as the
gauge-vector spectrum branch.  It records a partial D=10 reduction idea:
the off-diagonal \(\sqrt J\) can come from exact/coexact Hodge data, the
protected zero from gauge invariance, and the diagonal \(-J\) remains the
source computation.

`../physres6/calculations/branch_duality.md` records the branch involution
\[
x\mapsto -\frac{J}{x},
\qquad
x_+(J)x_-(J)=-J.
\]
The valid import is theorem-target material: identify the involution with a
source duality, or keep it as an algebraic branch relation inside the source
package.

### Brane/top provenance

`../phys4/notes/80-brane-string-dictionary.md` supplies a project-source bridge
from Chan--Paton endpoint labels to matrix gauge fields, Seiberg dual mesons,
Hanany--Witten brane moves, and the M-theory lift.  The manuscript may use this
as provenance for endpoint and brane addresses only.  Primary brane/string
sources remain required for strengthened claims.

`../phys4/notes/84-top-quark-brane-mode.md` supplies top-sector provenance for
the negative-branch and O10 discussion.  The useful import is structural:
a heavy flavor can appear as an asymptotic/global brane or M-theory datum whose
local low-energy sector carries a memory through matching data.  This is a
future source-map target for \(\mathcal Y_{\rm top}\), with numerical top
comparisons deferred to a calculation/source-audit phase.

### Wrong Turn genealogy

`../phys4/notes/135-pf-wrong-turn-catalogue.md` records the PhysicsForums
thread as project genealogy: SO(32), sBootstrap, Seiberg duality, brane
constructions, top exclusion, and mechanism-search pressure.  The valid import
is historical/provenance pressure and the explicit demand for a proof of
concept.  Any manuscript claim requires direct thread context or primary
literature.

## Loop 34 Consequence

The parent read reinforces the O4 implementation direction:

1. Name the CHM current-entry theorem as the immediate source-kernel target.
2. State the minimal source package:
\[
\mathfrak D_{\rm O4}^{\rm CHM}=
(I,g_5,K_T^{\rm DtN},K_T^{\rm brane},\langle\cdot,\cdot\rangle_{\rm CHM},
P_\gamma,P_{a,J},K^{\rm ref}_{T,J},\Lambda_{\rm CHM},\mathcal R_{\rm pole}).
\]
3. Keep endpoint/Chan--Paton as a matrix arena pending a worldvolume quadratic
   kernel and projection.
4. Keep \(G_2\) as a local-kernel route whose source audit must list the
   Higgs-bundle equations and local inner product.
5. Keep the Hodge/SUSY-QM route as the square-root entry theorem, with Witten's
   \(d_t\), \(d_t+d_t^\dagger\), and Hodge-Laplacian source equations audited
   as prerequisites for promotion.
6. Tie every source route to O1/O3/O8/O10/O20 through one light space,
   projection, source scale, scalar functional, dimensional variable, and
   pole map.
